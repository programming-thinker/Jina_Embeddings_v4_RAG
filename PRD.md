# 政府工作报告智能问答系统 PRD

| 项目 | 内容 |
|---|---|
| 版本 | v1.1（检索完整度优化） |
| 状态 | 已上线（单机 / API 可集成） |
| 文档类型 | 迭代型 PRD（针对已有 v1.0 的效果优化） |

---

## 1. 背景与问题定义

### 1.1 业务背景

31 个省区市每年发布政府工作报告，是政策研究、行业分析、区域对比的一手材料。原始形态是 31 份 Word 文档、单份 1.5–3 万字、总量约 60 万字。

人工处理的痛点是**跨文档的横向检索**:回答"哪些省把低空经济写进了 2025 年重点任务"需要通篇读完 31 份文档,人工耗时 2–4 小时,且容易漏。

### 1.2 v1.0 上线后的核心问题

v1.0 已实现基础 RAG 问答,但收到两类高频负反馈:

| 问题 | 用户原话 | 本质 |
|---|---|---|
| **P0 信息不完整** | "明明报告里写了,系统说没有" | 召回不足 —— top_k 过小,相关块被截断在候选集外 |
| **P0 对比不够细** | "对比 5 个省,每个省只给一句话" | 上下文预算被均分,单省份配额过低,量化指标丢失 |

### 1.3 归因

对 bad case 抽样归因后,确认**不是生成模型的问题,是检索层的预算问题**:

- 全局 `top_k = 20`,但一个省份的相关内容常分散在 5–10 个块中,多省份查询时单省只能分到 2–3 块
- 上下文上限 16,000 字符,而生成模型(QwenLong-L1-32B)本身支持远超此值的上下文——**能力没用满**
- 所有查询类型共用一套检索参数,"某省重点工作是什么"和"对比 5 省 GDP 目标"用同样的预算,显然不合理
- 块与块之间无上下文衔接,命中"2025年主要目标"标题块时,紧邻的数据块未被带出

### 1.4 关键约束

> **本次迭代不重建索引**(内部称"阶段 1 无重切块方案")。

重新设计切块策略需要重跑全量向量化 + 回归验证,周期长。本次先在**不动索引结构**的前提下,通过检索策略和上下文预算的调整拿到收益,验证方向后再决定是否投入切块重构。

---

## 2. 用户与场景

### 2.1 目标用户

| 角色 | 典型需求 | 使用频率 |
|---|---|---|
| 政策研究员 / 智库分析师 | 跨省政策横向对比、专题梳理 | 高 |
| 券商 / 咨询行业研究员 | 特定产业在各省的表述与目标 | 中 |
| 政企材料撰写人员 | 查本省及对标省份的表述口径 | 中 |
| 媒体记者 | 找选题、核实数据 | 低频高时效 |

### 2.2 核心场景(按检索复杂度分层)

| 场景 | 示例 Query | 涉及省份数 |
|---|---|---|
| S1 单省深度 | "河南 2025 年重点工作有哪些" | 1 |
| S2 多省定向 | "江苏、浙江、广东的数字经济部署" | 2–5 |
| S3 全省扫描 | "列出各省 GDP 增长目标" | 31 |
| S4 对比分析 | "对比广东和江苏的产业发展路径" | 2–5(需成对呈现) |
| S5 主题专项 | "哪些省提到了低空经济" | 不定 |

**这个分层是本次方案的设计基础**——不同场景的信息密度需求完全不同,必须差异化分配检索预算。

### 2.3 非目标(明确不做)

- ❌ 不做多轮对话 / 上下文追问(v1 定位单轮精准问答)
- ❌ 不做报告以外的数据源(统计年鉴、新闻)
- ❌ 不做政策解读与预测(仅做原文的检索与组织,不做推断)
- ❌ 不做实时更新(报告年度发布,离线重建索引即可)

---

## 3. 目标与成功指标

### 3.1 北极星指标

**答案完整率** —— 人工评测集上,系统回答覆盖了标准答案中全部关键信息点的比例。

### 3.2 指标体系

| 层级 | 指标 | v1.0 基线 | v1.1 目标 |
|---|---|---|---|
| **结果指标** | 答案完整率(全部要点召回) | 待补测 | ≥ 85% |
| | 关键数字准确率(量化指标不出错) | 待补测 | ≥ 95% |
| | 用户"信息不全"负反馈率 | 基线 | 下降 50% |
| **过程指标** | 检索召回率 Recall@k(相关块进入候选) | 待补测 | ≥ 90% |
| | 上下文利用率(实际填充/窗口上限) | 约 30% | ≥ 70% |
| **体验指标** | P90 响应时延 | 约 30s | ≤ 120s(可接受劣化) |
| | 单次查询 token 成本 | 基线 | ≤ 4x 基线 |

> ⚠️ **本文档诚实标注**:v1.1 上线时,结果指标尚未建立标注评测集,实际验收依赖 bad case 复测 + 用户反馈。这是本次迭代最大的方法论缺口,已列入 v1.2 首要事项(见 §7)。

### 3.3 明确的取舍

本次迭代**主动接受**以下劣化,换取完整度:

- 时延:超时阈值从 60s 放宽到 180s
- 成本:单次查询上下文最多 100K 字符,token 成本显著上升

理由:目标用户是研究场景,**"等 1 分钟拿到完整答案"远优于"3 秒拿到残缺答案"**。这类用户的替代方案是人工读 2 小时。

---

## 4. 功能需求

### 4.1 F1:查询意图识别与路由(P0)

系统需在检索前判定 query 归属的场景类型,并据此选择检索策略。

**输入**:自然语言 query
**输出**:`QueryPlan { 意图类型, 涉及省份列表, 输出格式, 复杂度 }`

判定维度:
- **涉及省份**:从 query 中抽取省份实体,无实体且含"各省/全国/所有"→ 全省扫描
- **意图类型**:对比类关键词(对比/相比/差异)→ 对比场景;统计类(多少/合计/排名)→ 汇总场景
- **输出格式**:决定后续 prompt 模板(列表 / 详述 / 表格 / 汇总)

**兜底**:识别失败时降级为通用检索(top_k=60),不阻断查询。

### 4.2 F2:分层检索预算(P0)

按 F1 的判定结果,差异化分配每省块数和字符预算:

| 场景 | 每省块数 | 上下文预算 | 设计依据 |
|---|---|---|---|
| S1 单省深度 | 30 | 40K 字符 | 单省可独占预算,追求单份报告的信息完整 |
| S2 多省定向 | 15 | 60K | 省份数少,单省仍需足够深度 |
| S3 全省扫描 | 8 | 80K | 31×8=248 块,单省浅但求全覆盖 |
| S4 对比分析 | 25 | 100K(上限 150 块) | 对比需要双方细节对齐,预算最高 |

**关键设计**:预算随涉及省份数**非线性**分配。省份越多、单省越浅,但总预算越大——保证"广度查询不因均分而失真"。

### 4.3 F3:相邻块聚合(P0)

命中某个块后,自动带出其前后相邻块(默认 window=1)。

**解决的问题**:标题块与数据块被切分开时,命中标题却拿不到数据。这是"读不全"的直接成因之一,且**无需重建索引**即可缓解——完美契合 §1.4 的约束。

### 4.4 F4:信息密度优先的截断(P0)

当召回内容超出上下文预算时,不做简单尾部截断,而是按信息密度打分保留:

- 含数字/百分比/量化指标的块 → 加权
- 与 query 语义相似度高的块 → 加权
- 纯过渡性、表态性文本 → 降权

**理由**:政府工作报告中大量篇幅是表述性语言,用户要的是数字和具体措施。截断策略直接决定答案质量上限。

### 4.5 F5:格式化输出(P1)

按 F1 判定的输出格式套用对应 prompt 模板:

| 格式 | 形态 | 适用 |
|---|---|---|
| 省份列表 | `省份:数据1、数据2...` | S3 全省扫描 |
| 详细报告 | 分点详述,保留全部量化指标 | S1 单省深度 |
| 对比表格 | 维度对齐的表格 | S4 对比 |
| 统计汇总 | 数据 + 分布分析 | 统计类 |

**强制要求**:prompt 中明确约束"必须保留原文中的所有数字",防止模型在长上下文下概括掉细节。

### 4.6 F6:API 服务化(P1)

提供 RESTful 接口供前端和第三方集成:

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/query` | POST | 提交查询,返回答案 + 元信息(命中省份、查询类型、耗时) |
| `/api/status` | GET | 系统状态与索引统计 |
| `/api/setup` | POST | 触发索引构建/重建 |
| `/api/health` | GET | 健康检查 |

---

## 5. 技术方案约束

```
docx 解析 → 分块(1000字/200重叠) + 省份标注
   → Jina Embeddings v4(本地 GPU)→ FAISS 索引        [离线,一次性]
─────────────────────────────────────────────────
Query → 意图识别 → 分层检索 + 相邻块聚合
   → 密度打分截断 → QwenLong-L1-32B(云端 API)→ 答案   [在线]
```

| 组件 | 选型 | 决策理由 |
|---|---|---|
| 向量模型 | Jina Embeddings v4(本地) | 中文表现好、8192 长度支持;本地部署规避数据外发与调用成本 |
| 向量库 | FAISS(CPU) | 数据量仅数千块,CPU 检索毫秒级,无需引入独立向量服务 |
| 生成模型 | QwenLong-L1-32B(硅基流动 API) | **长上下文是本方案的前提**,需消化 100K 字符输入;走 API 避免本地部署 32B 的硬件成本 |

**关键权衡**:embedding 本地 + 生成走云。本地只承载 3.8B 编码器(消费级显卡即可),把 32B 的算力成本转移到 API 按量付费。

**已知配置修正**:v1.0 中 `max_tokens` 配置为 20480,超出模型实际上限,长输出场景会触发截断。本次修正为 8192(模型支持的最大值)。

---

## 6. 风险与降级

| 风险 | 影响 | 应对 |
|---|---|---|
| 长上下文下模型"中间遗忘" | 塞得多不等于用得好,中段信息被忽略 | 按相关性排序后置放,高分块置于上下文首尾 |
| 时延超出用户耐受 | 用户流失 | 前端展示进度态;超时阈值放宽至 180s |
| token 成本失控 | 单次查询成本上升数倍 | 按场景分配预算而非一律拉满;监控单查询成本 |
| 意图识别错判 | 走错检索策略 | 降级为通用检索,不阻断 |
| 索引缺失时调用查询 | 返回"未就绪" | 部署时强制先完成索引构建再开放服务 |

---

## 7. 迭代规划

| 版本 | 内容 | 状态 |
|---|---|---|
| v1.0 | 基础 RAG 问答链路 | 已上线 |
| **v1.1** | **分层检索 + 相邻块聚合 + 密度截断(本文档)** | **已上线** |
| v1.2 | **建立标注评测集**(≥100 条 query,标注关键信息点),补齐结果指标基线 | 规划中 |
| v1.3 | 引入 rerank 精排,在同等上下文预算下提升信息密度(降本方向) | 规划中 |
| v1.4 | 切块策略重构(语义切分替代固定长度),解决 §1.4 遗留的根因 | 待评估 |
| v2.0 | 多轮对话、答案溯源(引用原文位置)、多年度纵向对比 | 待评估 |

---

## 8. 遗留问题

1. **评测缺失**:v1.1 的验收依赖 bad case 复测,缺少可复现的量化结论。这是当前最大缺口。
2. **堆量 vs 精排**:本次通过扩大上下文拿到收益,但这是"用成本换效果"的粗放解法。rerank 应能在更低预算下达到同等效果,v1.3 需验证。
3. **切块根因未解**:相邻块聚合是对固定长度切块的补丁,不是根治。语义切分才是正解,但需重建索引。
4. **API 无鉴权**:当前 CORS 全开且无 token 校验,仅适用于内网。对外暴露前必须补齐。
