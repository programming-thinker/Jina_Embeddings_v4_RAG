# AI PM 面试题库 —— 政府工作报告 RAG 项目

面试官会问的问题、弱答案与强答案对比、以及每题后面的追问链。

> 所有数字均来自本仓库实测(`mvp/`),可复现。
> 技术细节见 [`ARCHITECTURE_CN.md`](ARCHITECTURE_CN.md),本文只讲**怎么说**。

---

## 目录

- [第 0 部分:四个万能框架](#第-0-部分四个万能框架)(先读这个)
- [第 1 部分:项目定位](#第-1-部分项目定位)
- [第 2 部分:切块](#第-2-部分切块-chunking)
- [第 3 部分:向量化](#第-3-部分向量化-embedding)
- [第 4 部分:检索](#第-4-部分检索)
- [第 5 部分:上下文与生成](#第-5-部分上下文与生成)
- [第 6 部分:评估](#第-6-部分评估最容易挂的一节)
- [第 7 部分:工程与成本](#第-7-部分工程与成本)
- [第 8 部分:反思与收口](#第-8-部分反思与收口)
- [**第 9 部分:岗位定向 —— 字节豆包办公(金融评测方向)**](#第-9-部分岗位定向--字节豆包办公-ai-pm金融评测方向)
- [附 A:数据卡](#附-a数据卡)
- [附 B:雷区清单](#附-b雷区清单)
- [附 C:30 秒项目自述](#附-c30-秒项目自述)
- [附 D:简历怎么写](#附-d简历怎么写)(投递前先看这个)

---

## 第 0 部分:四个万能框架

**不要背 40 道题的答案。** 背四个框架,现场套。

面试官的问题只有四类,每类一个套路。

### 框架 ①:数字类 —— "为什么是这个值?"

> 覆盖:为什么切 1000 字、为什么重叠 200、为什么 top_k=30、为什么 temperature=0.3

**三段式:坦白来源 → 实测依据 → 正确定法**

```
"这个值当时是拍的（或沿用的默认值），没有做过参数搜索。
 但我后来实测了 ——（给一个具体数字）。
 如果重来，我会用 ——（说出可验证的定法）来定。"
```

**为什么这个结构有效:** 面试官不指望一个 PM 能说出"我做了网格搜索"。他要判断的是——你**知不知道这个值应该怎么定**。承认拍脑袋不扣分,说不出正确定法才扣分。

**致命错误:** 硬编一个理由("1000 字是因为语义完整性最佳")。追问一句"你怎么验证的"就穿帮。

### 框架 ②:选型类 —— "为什么用 A 不用 B?"

> 覆盖:为什么 FAISS、为什么 Jina v4、为什么本地部署、为什么不上 rerank

**四段式:约束 → 候选 → 判据 → 代价**

```
"当时的约束是 ——（数据量/预算/时间/硬件）。
 候选有 A 和 B。
 我按 ——（判据）选了 A。
 代价是 ——（说出你放弃了什么）。"
```

**关键在最后一句。** 只说优点的人像在背文档;能说出代价的人像真做过决策。

### 框架 ③:效果类 —— "你怎么证明有效?"

> 覆盖:怎么衡量、recall 多少、优化效果如何、bad case 怎么处理

**三段式:诚实说验收方式 → 指出方法本身的缺陷 → 给出正解**

```
"当时是 ——（bad case 复测 / 用户反馈）。
 这个方法的问题是 ——（说出方法论缺陷）。
 正确做法是 ——（评测集怎么建、算什么指标）。"
```

**绝对不要编指标。** 编了必被追问"评测集多大""怎么标注的""标注一致性如何",三层之内一定塌。

### 框架 ④:反思类 —— "最大的问题是什么 / 重做会怎么做?"

> 覆盖:项目缺陷、改进方向、学到了什么

**一条铁律:说架构级判断,不说小 bug。**

```
❌ "有个地方 top_k 设小了"          → 太琐碎，显得没高度
❌ "代码里有几个 bug"                → 面试官不关心
✅ "这个场景其实不需要 RAG"          → 架构级判断，有说服力
✅ "RAG 有适用规模，我当时没意识到"   → 可迁移的认知
```

---

## 第 1 部分:项目定位

### Q1. 一句话介绍这个项目

**考察:** 能不能讲清价值,而不是罗列技术。

❌ **弱答:** "我做了一个基于 Jina Embeddings v4 和 FAISS 的 RAG 系统,用了分层检索架构。"
→ 全是技术名词,听不出解决了什么问题。

✅ **强答:**
> "31 个省的政府工作报告,60 万字。政策研究员要做跨省对比,以前得通读所有文档,两到四个小时。这个系统让他问一句话、一分钟拿到答案,而且每个数字都能追溯到原文。
>
> 我在里面主要做的不是搭链路,是**检索层的归因和优化**——系统上线后用户反馈'答不全',我去定位并解决这个问题。"

**🔻 追问:** "为什么不能直接用 ChatGPT 问?"
> "三个原因:一是模型没见过这些报告的具体数字;二是它不会说不知道,会编一个看起来合理的数字;三是 60 万字塞不进去。RAG 的价值就是把原文摆在模型面前,让它照着答、不许发挥。"

### Q2. 用户是谁?真实需求是什么?

✅ **强答:**
> "主要是政策研究员、券商行业研究员,还有写材料的人。
>
> 他们的核心需求不是'读懂一份报告'——那个自己看就行。是**跨文档的横向检索**:'哪些省把低空经济写进了 2025 年重点任务'。这个问题人工要翻 31 份,而且容易漏。
>
> 我实测过,这个词 **29/31 个省**都提到了。漏一两个省,结论就变了。"

### Q3. 为什么用 RAG?不用行不行?

**考察:** 这是个陷阱题。答"必须用 RAG"的人没想清楚。

✅ **强答(这题答好能直接加分):**
> "**分情况,而且这正是我这个项目最大的反思。**
>
> - 单省查询,1.3 万字——长上下文模型本来就吃得下,**不需要 RAG**,直接给全文更简单更准
> - 2 到 5 个省,6 万字——也还能塞下
> - 31 个省,60 万字——**这才是 RAG 必须出场的地方**
>
> 我当时没做这个区分,所有场景都走检索。后来实测才发现,单省查询时检索层实际上没起作用。"

**🔻 追问:** "为什么没起作用?"
→ 转到 Q13,这是全场最重要的一题。

---

## 第 2 部分:切块 (Chunking)

### Q4. 为什么切 1000 字?这个数怎么定的?

**考察:** 框架①。面试官在看你会不会编。

❌ **弱答:** "1000 字是中文语义比较完整的长度,能保证上下文连贯。"
→ 听起来专业,但是编的。追问"你对比过 500 和 1500 吗"就完了。

✅ **强答:**
> "**说实话,这个值是经验值,没做过参数搜索。**
>
> 但我知道它的权衡在哪:切太大,一次塞不进几块,而且会混进无关内容稀释相关性;切太小,句子被腰斩,语义不完整。1000 字在中文里大概是 2~3 个自然段,是个常见的起点。
>
> 我后来实测了实际效果:31 份报告切出 **855 个块**,块长分布是最短 303、中位 874、最长 1543。
>
> 如果重来,我会先建评测集,再用 500 / 1000 / 1500 三组跑同一批 query,比信息点召回率——**用数据定,而不是拍。**"

**🔻 追问 1:** "为什么块长浮动这么大?最短 303 最长 1543?"
> "因为切块**不是按字符硬切,是按段落聚合**。逐段累加,超过 1000 就封块,所以实际长度取决于段落边界。好处是不会把句子劈开,代价是长度不可控。"

**🔻 追问 2:** "块长不均匀会有什么问题?"
> "会影响相似度打分。长块包含的信息多,和任何 query 都有一定相关性,容易被检索出来;短块更精准但容易被埋没。理想情况是做长度归一化,这个项目没做。"

### Q5. 为什么重叠 200 字?不重叠会怎样?

✅ **强答:**
> "防止一句话被切断。比如'2025 年 GDP 增长目标为'在上一块结尾,'5.5%'在下一块开头——两块单独看都没意义,检索时哪块都命中不了。
>
> 200 字是 1000 的 20%,是常见比例。
>
> 代价是**存储和计算的冗余**。我实测过河南:原文 12,975 字,切成 20 块之后各块字数相加是 **16,775 字**,多出来的 3,800 字就是重叠。相当于 29% 的冗余。"

**🔻 追问:** "有没有不用重叠也能解决这个问题的办法?"
> "有,语义切分——按标题、段落层级这些自然边界切,而不是按固定长度。这样切点本身就在语义边界上,不需要靠重叠去补。代价是要重建索引,而且实现复杂度高不少。"

### Q6. 有没有做块的分类?

✅ **强答:**
> "做了,分 title / content / target / summary 四类,想法是按类型做定向检索。
>
> **但我实测发现这个分类基本失效了。** 855 个块里 `title` 类型是 **0 个**——判定条件写的是'长度小于 100 字',而实测最短的块是 303 字。**切块粒度和判定条件互斥,永远不可能命中。**
>
> `target` 类型占了 55.7%,所以按 target 过滤只筛掉了 44% 的块,过滤意义不大。
>
> 这是个典型的教训:**功能上线了不等于生效了**,不实测根本发现不了。"

**🔻 追问:** "那你会怎么改?"
> "两条路。短期:把判定条件改成基于块内的结构特征,比如是否含中文数字编号开头。长期:分类这件事本身应该在切块时就用文档结构信息做,而不是切完之后靠正则猜。"

---

## 第 3 部分:向量化 (Embedding)

### Q7. 为什么选 Jina Embeddings v4?

**考察:** 框架②。这题是选型题的典型代表,**答完标准答案还要主动亮反思**,否则拿不到高分。

#### 先搞清楚 Jina v4 是什么

答之前你得知道自己用的是什么东西。一句话:**Jina AI 2025 年 6 月发布的多模态检索向量模型,底座是 Qwen2.5-VL-3B。**

| 属性 | 值 |
|---|---|
| 参数量 | 约 38 亿(磁盘 7.5GB,对得上 bf16 精度) |
| 底座 | Qwen2.5-VL-3B-Instruct(视觉语言模型) |
| 上下文长度 | 32K tokens |
| 输出 | 单向量 2048 维(支持 Matryoshka 截断)/ 多向量(late interaction) |
| 语言 | 30+ 种,中文表现好 |
| 独特能力 | **多模态**——能直接编码图片和 PDF 截图,不用先 OCR |

三个关键设计:

1. **任务专用 LoRA 适配器** —— 同一底座挂 `retrieval` / `text-matching` / `code` 三套适配器,按任务加载
2. **非对称检索** —— 查询侧和文档侧走不同前缀(详见 Q9,这是本项目踩坑的地方)
3. **多模态** —— 整页 PDF 当图片编码,保留表格与版式

#### 同期还有哪些候选

| 模型 | 出品方 | 参数 | 上下文 | 部署 | 特点 |
|---|---|---|---|---|---|
| **Jina v4** | Jina AI | 3.8B | 32K | 本地 | 多模态、任务 LoRA、非对称 |
| **BGE-M3** | 智源 BAAI | 568M | 8K | 本地 | **密集+稀疏+多向量三合一**,中文强,轻 |
| **Qwen3-Embedding** | 阿里 | 0.6B/4B/8B | 32K | 本地 | 同期发布,多语言榜单靠前,尺寸可选 |
| **gte-Qwen2-7B** | 阿里达摩院 | 7B | 32K | 本地 | 大而强,但 15GB 更重 |
| **BGE-large-zh** | 智源 BAAI | 326M | **512** | 本地 | 极轻极快,上下文太短 |
| **text-embedding-3** | OpenAI | — | 8K | **API** | 省事,但数据出境 |
| **Cohere / Voyage** | 海外 | — | — | **API** | 质量好,国内访问不便 |

#### ✅ 标准答法(框架②:约束 → 候选 → 判据 → 代价)

> "**约束**三条:中文语料、要支持长文本、数据不能出本地。
>
> **候选**:BGE-M3、Qwen3-Embedding 这些本地模型,以及 OpenAI、Cohere 这类 API 方案。
>
> **判据**:先用'能否本地部署'砍掉所有 API 方案——这是硬约束不是偏好,政府报告虽然公开,但客户不接受把全部语料上传第三方。剩下的本地模型里选了 Jina v4,看中中文表现、长上下文,以及多模态能力——考虑到未来可能要处理带表格的 PDF 扫描件,这个能力有前瞻价值。
>
> **代价**:7.5GB 权重、必须配 GPU,部署门槛从 pip install 变成下载 7.5GB 加配 CUDA。而且本地部署意味着模型更新要自己跟。"

**为什么不选别的:**

| 不选 | 理由 |
|---|---|
| OpenAI / Cohere API | **数据出境是红线**,一票否决。且按量持续付费,长期高于一次性硬件投入 |
| BGE-large-zh | **512 token 上下文太短**,块中位数 874 字会被硬截断 |
| gte-Qwen2-7B | 15GB,比 Jina v4 重一倍,收益不明显 |
| M3E | 2023 年的模型,已被 BGE 系列全面超越 |

#### ⭐ 加分项:主动承认过度选型

**这部分才是拉开差距的地方。** 把 Jina v4 的能力和项目实际需求对照:

| Jina v4 的能力 | 这个项目用上了吗 |
|---|---|
| 多模态(图片/PDF 直接编码) | ❌ **完全没用** —— 语料是 docx 抽出的纯文本 |
| 32K 上下文 | ❌ **严重浪费** —— 块中位数才 874 字 |
| 任务 LoRA(code / text-matching) | ❌ 只用了 retrieval 一个 |
| 非对称检索 | ❌ **用错了** —— 查询侧误走 passage 前缀(见 Q9) |

**四个卖点一个都没吃到,代价却实打实付了。**

> "**复盘下来这个选择偏重了。** Jina v4 的多模态能力完全没用到——语料是纯文本;32K 上下文也用不上——块中位数才 874 字。我们付出了 7.5GB 权重和必须配 GPU 的代价,却没吃到它任何一项独特能力。
>
> 如果重做,我会认真评估 **BGE-M3**:568M 参数、2.2GB,轻三倍多;8K 上下文对 874 字的块绰绰有余;中文是它的主场。
>
> **最关键的是**——它原生支持稀疏+密集混合检索。政府报告里全是'GDP 增长 5.5%''新增就业 1200 万人'这类精确数字,稀疏检索在精确匹配上比纯语义向量更可靠,而这恰恰是'答不全'问题的一个根因。"

**为什么这个答法有力量:** 面试官问选型题,考的从来不是"你知不知道这个模型",而是"**你会不会做技术决策**"。能说出"方向对了但过度选型,而且我知道更契合的替代方案是什么、为什么",这已经超出大多数 PM 候选人的水平。

**🔻 追问 1:** "为什么不用 OpenAI 的 embedding API?省事。"
> "数据出境是第一条红线。第二个原因是成本结构——建库要编码 855 个块,每次查询还要编码 query,调 API 是按量持续付费;本地部署是一次性硬件投入。这个项目的查询量下,本地更划算。"

**🔻 追问 2:** "那你做过模型横向评测吗?"

⚠️ **这题必须诚实。** 仓库里没有任何选型决策文档——没有 ADR、没有设计文档,README 只陈述"用了 Jina v4"。所以上面那套"约束→候选→判据"是**基于项目实际情况的合理重构,不是当时的真实决策记录**。

❌ **千万别说:** "我们当时做了详细的模型对比评测。"
→ 追问"评测数据呢""在什么数据集上测的""指标是多少",你拿不出来,前面所有可信度一起塌。

✅ **该说:**
> "**没有做系统的横向评测**,当时主要看中的是本地部署和中文表现,属于经验判断。这也是我复盘时觉得该补的——选型和调参一样,都应该先有评测集再做决定,否则就是拍脑袋。"

### Q8. 向量是多少维?

**⚠️ 这题容易翻车。**

❌ **弱答:** "1024 维。"
→ 代码里 `vector_store.py:27` 的 `embedding_dim=1024` **只是个默认参数**,`:120` 会用 `embeddings.shape[1]` 覆盖掉。你说死一个数,面试官如果懂,会追问"你确认过吗"。

✅ **强答:**
> "**维度是运行时探测的,不是写死的。** 代码里有个 1024 的默认值,但建索引时会用模型实际输出的 `shape[1]` 覆盖。
>
> 这么设计是因为换模型不用改配置。项目里还专门有个 `rebuild_index.py` 脚本,就是解决维度不匹配的——先问模型要真实维度,再清空旧索引重建。"

**这个答法比报一个数字强得多**,因为它展示了你读过代码。

### Q9. query 和文档用同一套编码方式吗?

**考察:** 这题能问出来的面试官是懂行的。答好了是全场亮点。

✅ **强答:**
> "**应该分开,但这个项目用错了。**
>
> Jina v4 是**非对称检索模型**,有 `query` 和 `passage` 两套前缀。因为提问和答案的语言形态不一样——问题是'河南今年要干啥',答案是'2025 年主要目标为...',模型要学会把这两种说法对上号。
>
> 代码里给文档编码用的是 `passage`(对的),但给 **query 编码时没传参数,走了默认值,也是 `passage`**。
>
> 更讽刺的是,写法正确的 `encode_query()` 函数就在 `embedding_manager.py:253`,**一次都没被调用过**。
>
> 这意味着**排序质量本身是打折的**,而我当时的解法是把 top_k 从 20 调到 60——**用扩大候选集去掩盖排序质量问题**。这是我复盘时最受触动的一条。"

**🔻 追问:** "你验证过修好之后效果提升多少吗?"
> "**没有,这是我目前还欠的。** 验证需要真实的 Jina 权重和评测集,两个我都还没有。我能确认的是静态事实——`encode_query()` 在主链路零调用。要量化影响,得先补上评测集。"

*(诚实说没验证,比编一个数字强一百倍)*

---

## 第 4 部分:检索

### Q10. top_k 是怎么定的?

✅ **强答(框架①):**
> "不是一个值,是**按查询类型分层配的**:
>
> | 场景 | 每省块数 | 上下文预算 |
> |---|---|---|
> | 单省深度 | 30 | 40K 字符 |
> | 多省定向 | 15 | 60K |
> | 全省扫描 | 8 | 80K |
> | 省份对比 | 25(总量帽 150) | 100K |
>
> 设计逻辑是**非线性分配**:省份越多,单省分到的越浅,但总预算越大——保证广度查询不会因为均分而每个省只剩一句话。
>
> **但具体数值是拍的,没做过参数搜索。** 而且我后来发现单省那个 30 有个严重问题。"

*(主动把话头引到 Q13,这是你的高光时刻)*

### Q11. 这些数字之间是什么关系?

✅ **强答:**
> "两个约束在互相拉扯:**块数**决定覆盖多少内容,**字符预算**决定能塞进模型多少。
>
> 有意思的是这两者并不总是一致。比如对比场景配的是每省 25 块,但如果真去对比 31 个省,25×31=775 块,远超 150 块的总量帽——**per-province 配额在大范围对比时形同虚设**,实际生效的是那个硬帽。这说明配额体系本身设计得不够严密。"

### Q12. 全省查询每省只给 8 块,够吗?

✅ **强答:**
> "31×8=248 块,配置注释里也是这么算的。
>
> 但**实际拿不到 248**。我实测了 5 条查询,实得 178~192 块,**平均缺口 26%**。
>
> 原因是省份过滤是**后置**的——代码先取全局 top-200,再从里面筛某个省的块。如果某个省的块没进全局 top-200,这个省就分不满配额。"

**🔻 追问:** "那会不会有省份一块都拿不到?"
> "**在当前数据规模下不会**,855 块的时候全局 top-200 已经覆盖了 23% 的全量,足以触及每个省。
>
> 但我做了规模敏感性测试——`search_k` 是**硬编码 200**,不随索引增长:
>
> | 索引规模 | 缺口 | 0 块省份 |
> |---|---|---|
> | 855 | 22.6% | 0 |
> | 4275 | 29.0% | 0 |
> | **8550** | **37.9%** | **10 个** |
>
> 也就是说多年度报告入库后,会有 10 个省整省查不到。**这不是已经爆发的 bug,是随规模逼近的定时炸弹。**"

**🔻 追问:** "怎么修?"
> "改成 pre-filter——先按省份圈定候选集,再在集合内排序。实现上可以按省分建子索引,或者用 FAISS 的 `IDSelector`。"

### Q13. ⭐ 单省查询实际返回多少个块?

**这是全场最重要的一题。答好了整场面试就稳了。**

✅ **强答:**
> "**这题的答案很反直觉:配额写的是 30,但实际返回的是这个省全部的块。**
>
> 因为——各省的块数中位数只有 28。河南 12,975 字,切出来 **20 个块**。
>
> **配额 30 > 实际拥有 20 → 全部返回。**
>
> 而且不是个例,**31 个省里有 22 个省总块数不到 30**。
>
> 这意味着单省查询时,排序、过滤、打分**全部不产生作用**——等于把这个省的报告**全文投喂**给模型。
>
> 这解释了当初'调大 top_k 后召回问题就消失了'的真实机制:**不是检索变准了,是绕过了检索。** 时延和 token 成本的上涨也同源于此。"

**🔻 追问:** "那你觉得应该怎么做?"
> "按查询范围分流:
>
> | 范围 | 做法 |
> |---|---|
> | 单省(1.3 万字) | 直接取全文,不走检索 |
> | 2~5 省(6 万字) | 同上,长上下文吃得下 |
> | 全省/主题 | 走 RAG |
>
> 更根本的认知是:**RAG 不是越用越好,它有适用规模。** 在数据量撑不起检索的时候,检索层就是纯开销。"

### Q14. 相似度是怎么算的?

✅ **强答:**
> "用的是 FAISS 的 `IndexFlatL2`,L2 距离,然后用 `1/(1+距离)` 转成相似度。
>
> **这里有两个可以商榷的地方:**
>
> 一是入库向量**没有做 L2 归一化**。未归一化的情况下 L2 距离和余弦相似度不等价,长文本的向量模长更大,距离会系统性偏大——**长块可能被系统性降权**,而政府报告里信息密集的往往就是长块。
>
> 二是配置里有个 `similarity_threshold=0.7`,但**代码里没有任何地方读它**,是个死配置。"

### Q15. 为什么用 FAISS?为什么不用向量数据库?

✅ **强答(框架②):**
> "**约束**是数据量只有 855 个块,单机部署,没有多租户和权限需求。
>
> **候选**:Milvus / Qdrant / Pinecone 这类向量数据库,或者 FAISS 这种库。
>
> **判据**:855 个块在 CPU 上做全量暴力检索是毫秒级。引入独立的向量服务只会增加运维复杂度,不带来收益。
>
> 具体索引类型选的是 `IndexFlatL2`——暴力精确检索,不做聚类近似。代码里 IVF 的写法以注释形式保留着,意思是数据量大了再换。
>
> **代价**:没有持久化服务、没有并发控制、索引更新只能全量重建。数据量涨一两个数量级就得换。"

### Q16. 要不要上 rerank?

✅ **强答:**
> "**该上,当时没上,原因是迭代节奏。**
>
> 当时有个约束:这次迭代不重建索引。加 rerank 要引入新模型、调阈值、重新验证,周期长。而扩大检索预算是纯配置改动,当天能验证。
>
> **先用最低成本验证'确实是召回不足'这个判断对不对,方向对了再投入做精排。**
>
> rerank 的价值在于降本——同样的效果可以用更小的上下文装下,成本和时延都更好。我把它放在下一个版本,定位就是降本。"

*(这个答法把"没做"从技术短板转成了迭代节奏的选择)*

---

## 第 5 部分:上下文与生成

### Q17. 100K 字符的上下文预算怎么定的?

✅ **强答:**
> "反推的:**上限来自模型能力,下限来自场景需求。**
>
> 选了 QwenLong-L1-32B,它支持超长上下文,这是前提。然后按场景分配——对比场景最吃上下文,给到 100K,恰好顶到全局上限。
>
> 但坦白说,100K 这个数**没有做过效果曲线**。理想做法是测不同上下文长度下的答案完整率,找到收益拐点。塞得多不等于效果好。"

**🔻 追问:** "为什么塞得多不等于效果好?"
> "长上下文有'中间遗忘'问题——模型对开头和结尾的注意力强,中段容易忽略。所以应该把高相关的块放在首尾。这个项目按省份分组拼接,没有按相关性排布,是可以优化的点。"

### Q18. Prompt 是怎么设计的?

✅ **强答:**
> "固定五段式:任务说明 →【用户问题】→【输出要求】→【重要原则】→【参考资料】→ 收尾指令。
>
> 两个设计点:
>
> 一是**检索到的原文放在最后、紧邻收尾指令**,利用模型对末尾的注意力。
>
> 二是**每套格式指令里都有一条'不要编造'**。因为这个场景对幻觉零容忍——用户要的是政策原文里的数字,编一个看起来合理的数字比说不知道危害大得多。
>
> 按输出格式分了四套指令:省份列表、详细报告、对比表格、统计汇总。"

### Q19. temperature 为什么设 0.3?

✅ **强答:**
> "降低随机性。这个场景要的是**忠实复述原文里的数字**,不是创造性表达。温度高了模型容易改写数字或者自由发挥。
>
> 0.3 也是经验值。严格来说应该测 0 / 0.3 / 0.7 三档在评测集上的数字准确率——但同样,没有评测集就测不了。"

### Q20. max_tokens 8192 是怎么回事?

✅ **强答:**
> "这里其实是个**修正**。原来配的是 20480,但**超过了模型实际支持的上限**,长输出场景会触发截断。后来改成 8192,是模型支持的最大值。
>
> 这算是个小教训:配置值要对着模型文档核,不能凭感觉写。"

*(主动说出自己发现并修正的错误,是加分项)*

---

## 第 6 部分:评估(最容易挂的一节)

### Q21. ⭐ 你怎么证明优化有效?

**这题是 AI PM 面试的分水岭。**

❌ **致命弱答:** "检索量提升了 200%,上下文提升了 525%,信息完整度提升 100%。"
→ **这些全是过程指标,不是效果指标。** top_k 从 20 调到 60 是**输入**,不是**产出**。面试官下一句一定是:"那效果提升了多少?"

✅ **强答(框架③):**
> "**说实话,这是这个项目最大的方法论缺口。**
>
> 当时的验收方式是 **bad case 复测**——收集用户报上来的失败样例,改完重跑,看修好了几个,再加上线后的负反馈率对比。
>
> 这只能证明'**那批坏 case 修好了**',不能证明整体召回率提升了。我手上没有可复现的量化结论。
>
> 如果重做,我会**先建评测集再动手**:抽 100 条真实 query,人工标注每条的关键信息点,算**信息点召回率**。这样才能区分'检索没召回'和'召回了但模型没用上'——这两个问题的解法完全不同,前者调检索,后者调 prompt。"

### Q22. bad case 复测有什么问题?

✅ **强答(能答出第二点就赢了):**
> "**三个缺陷:**
>
> **一是没有分母。** 只能说修好了 18 个,说不出整体好了多少。
>
> **二是看不见的错永远看不见——这条对 RAG 尤其致命。**
>
> 我举个实测的例子:语料里 **31 个省有 29 个都提到了'低空经济'**。假设用户问'哪些省提到了低空经济',系统答了 8 个省,每个都有理有据。
>
> **用户会报 bad case 吗?不会。** 因为答案看起来完全正常,他根本不知道应该有 29 个。
>
> 关键在这:**'说错了'用户能发现——去翻原文数字对不上;'漏说了'用户发现不了——他不知道该有多少。**
>
> 而 RAG 最主要的失效方式恰恰是**漏**。所以 bad case 这套方法会**系统性地低估**问题严重程度。
>
> **三是容易过拟合。** 手里就 20 道题,反复调参让这 20 道全对,跟背答案没区别。而且样本太少,没法做回归——改动可能修好 A 同时弄坏 B。"

### Q23. 评测集具体怎么建?

✅ **强答:**
> "**四步。**
>
> **① 出题 100 条**,按场景配比:单省深度 25 / 多省定向 20 / 全省扫描 20 / 省份对比 20 / 主题专项 15。
>
> **② 标注答案 —— 关键是标成信息点清单,不是标一段话。**
>
> ```
> 题目:河南 2025 年主要经济目标是什么
> 标准答案:
>   □ GDP 增长 5.5% 左右
>   □ 一般公共预算收入增长 3%
>   □ 城镇调查失业率 5.5% 以内
>   ... 共 8 个信息点
> ```
>
> 拆成点才能算分:答出 6 个 → 完整率 75%。
>
> **③ 算三个指标:**
>
> | 指标 | 抓什么 |
> |---|---|
> | 答案完整率 | 漏(主要矛盾) |
> | 关键数字准确率 | 错和编 |
> | 检索召回率 Recall@k | **分清病根在哪一层** |
>
> 第三个最重要。检索召回率高但完整率低 → 资料捞到了模型没用好 → 改 prompt;检索召回率低 → 压根没捞到 → 改检索。**没有这个指标只能靠猜。**
>
> **④ 每次改动都重跑**,当回归测试用。"

### Q24. 没有评测集的时候,怎么自证改动有效?

✅ **强答:**
> "退而求其次,我实际做过的是**结构性验证**——不测效果,测**机制**。
>
> 举两个例子:
>
> 一是截断算法。代码想按'信息密度'保留内容,但实测发现打分函数里 `content_score` 用 `.split()` 数词——**英文按空格分词,中文没空格**,所以中文永远数出来接近 1。实测取值 0.02~0.15,而同一个式子里另一项取值 0~2.0,**差两个数量级**,等于完全不参与排序。
>
> 我用样本验证了后果:数字最密集的那个块(甘肃,含 58 个数字)在原版排 **第 34/400**,加上数字密度后排 **第 1**。
>
> 二是意图识别。项目里有两处独立实现,我拿 6 条查询对比,**3 条判定不同**。
>
> 这类验证证明不了'效果提升多少',但能证明'**这里确实有问题**',而且可复现。**是评测集的下位替代,不是替代品。**"

### Q25. 幻觉怎么控制?

✅ **强答:**
> "三层:
>
> **Prompt 层** —— 四套输出模板每套都有'不要编造'的硬约束,温度压到 0.3。
>
> **检索层** —— 这才是根本。模型只能看到你给它的内容,**检索准了,幻觉的空间就小**。反过来说,检索漏了,模型面对缺失信息更容易编。
>
> **验证层** —— 这层项目没做。应该做答案溯源:每个数字标出处块 ID,让用户能一键跳原文。
>
> 溯源做不了的原因是 `start_pos` 字段算得不准——切块时处理重叠的加减逻辑有问题,和原文真实偏移对不上。想做溯源得先修这个。"

---

## 第 7 部分:工程与成本

### Q26. 时延瓶颈在哪?

✅ **强答:**
> "P90 大概 90 秒以上,比优化前的 30 秒明显劣化。三块:
>
> **一是 LLM 生成**,最大头。上下文最多 100K 字符,超时阈值放到了 180 秒。
>
> **二是重复编码,这块是纯浪费。** 全省查询要循环 31 个省,而**每次循环都把同一个 query 重新编码一遍**——同一个字符串编码 31 次。按项目自己标的性能估算,白耗 10~15 秒。把编码提到循环外就能省掉。
>
> **三是串行批次。** 多批次查询是纯 `for` 循环,没有并发。4 个区域批次各调一次 LLM,最坏情况能到分钟级。"

**🔻 追问:** "用户能接受 90 秒吗?"
> "研究场景能接受,判断依据是:**用户的替代方案是人工读 2 小时**。等 1 分钟拿到完整答案,远好于 3 秒拿到残缺答案——残缺答案的真实成本是他还得自己去翻原文,等于没省事。
>
> 但这个结论只在这个场景成立。如果是 C 端搜索或客服,同样的取舍就是错的,那种场景我会反过来砍上下文保时延。"

### Q27. 成本怎么算?怎么降?

✅ **强答:**
> "成本结构是**本地 + 云端混合**:
>
> - Embedding 在本地 GPU,一次性硬件投入
> - 生成走硅基流动 API,按 token 计费
> - 向量检索在 CPU,可忽略
>
> 大头是生成端。单省查询要喂进 16,775 字,全省对比最多 100K 字符。
>
> **三个降本方向,按性价比排:**
>
> 1. **单省查询不走 RAG**——省掉检索开销,但 token 没省(反正都是全文)
> 2. **上 rerank**——同等效果下把上下文压回去,这是真正降 token 的
> 3. **修掉重复编码**——省时延不省钱,但改动只要 10 行"

### Q28. QPS 涨 100 倍怎么办?

✅ **强答:**
> "现在的架构撑不住,得分层改:
>
> **检索层** —— `IndexFlatL2` 是暴力检索,855 块无所谓,量大了要换 IVF 或 HNSW。索引也得从单机 FAISS 换成独立向量服务。
>
> **服务层** —— 现在 RAG 实例是模块级单例,在导入时构造。文档推荐的 `--workers 4` 实际上会产生 4 个互不相干的进程级单例,而初始化接口只会命中其中一个。**这个架构下多 worker 是坏的**,得先改成共享索引 + 无状态服务。
>
> **生成层** —— 瓶颈会变成 API 配额和成本。要上缓存(高频 query 直接命中)、要做请求合并。
>
> 但说实话,**这个产品的用户量级下 QPS 不会涨 100 倍**。真要涨,我会先问是不是场景变了。"

### Q29. 报告每年更新,索引怎么维护?

✅ **强答:**
> "现在是**全量重建**,有个 `rebuild_index.py` 脚本:清空旧索引 → 重新切块 → 重新编码 → 重建。
>
> 855 个块的规模,全量重建可以接受。
>
> 但有两个隐患:一是**重建期间服务不可用**,没有双缓冲;二是脚本直接删目录下所有文件,**没有备份也没有二次确认**,路径配错会误删。
>
> 多年度数据入库后,更该关心的是**规模效应**——前面说的那个 `search_k` 硬编码 200 的问题,到 8550 块时会有 10 个省整省查不到。**索引更新策略要和检索策略一起改,不能只改一个。**"

---

## 第 8 部分:反思与收口

### Q30. ⭐ 这个项目最大的问题是什么?

✅ **强答(框架④):**
> "**架构选型层面的:这个场景的主要查询类型,其实不需要 RAG。**
>
> 配置给单省查询 30 个块的配额,但 31 个省里 22 个省的总块数不到 30。单省查询时检索层不产生任何筛选作用,等于全文投喂。
>
> 而单省报告只有 1.3 万字,长上下文模型本来就吃得下。
>
> **我当时的认知是'RAG 是标配',没有意识到 RAG 有适用规模。** 在数据量撑不起检索的时候,检索层就是纯开销——增加了复杂度、时延和成本,却没带来收益。"

### Q31. 重做会怎么做?

✅ **强答:**
> "**顺序会完全反过来。**
>
> 当时是:发现问题 → 改代码 → 看 bad case 好没好。
>
> 重做会是:
>
> **① 先建评测集**(100 条 query,信息点标注)——没有这个,后面所有判断都是猜
>
> **② 按范围分流**——单省和少数省直接全文,只有跨省和主题查询走 RAG
>
> **③ 修检索质量而不是加检索量**——先把 query/passage 前缀用对,再考虑加 rerank
>
> **④ 每步都在评测集上验证**
>
> 一句话:**先定义'怎么算好',再开始做。**"

### Q32. 你从这个项目学到了什么?

✅ **强答(准备两条,一技术一方法论):**
> "**技术上:** RAG 出问题时,先验证**排序质量**,再谈**召回数量**。排序坏的时候扩大 top_k 只是把噪声一起捞进来,还顺带推高成本和时延。我当时正好做反了。
>
> **方法论上:** AI 产品和普通产品最大的区别,是**要先定义'怎么算好',才能开始做**。传统功能能不能用肉眼可见,AI 的效果只存在于统计里——不建评测集,就等于蒙着眼睛开车,改对了改错了都不知道。"

### Q33. 给你 3 个月和 2 个工程师,你做什么?

✅ **强答:**
> "**第 1 个月:补评测。** 100 条 query 标注,建立完整率、数字准确率、检索召回率三个指标的基线。这一个月不写功能代码。
>
> **第 2 个月:按范围分流 + 修 P0。** 单省走全文;修 query 前缀、重复编码、post-filter 三个问题。每改一项在评测集上验一次。
>
> **第 3 个月:上 rerank 降本 + 做溯源。** rerank 的目标是同等效果下把上下文压回去;溯源要先修 `start_pos` 的偏移计算。
>
> **排这个顺序的理由:第一个月不产出功能,但没有它后面两个月都是白干。**"

---

## 第 9 部分:岗位定向 —— 字节豆包办公 AI PM(金融评测方向)

### 9.0 先看懂这个 JD 在招什么人

职责三条里,**第 2 条才是这个岗位的主干**:

> "构建针对金融等专业领域的 AI 效果评测集,通过对输出结果标准的定义与质量比对,发现模型在逻辑推理、金融规范、复杂计算上的短板"

任职要求最后一条是它的镜像:

> "对评测工作中的负面案例有敏感度和钻研精神"

**翻译成人话:这是一个「找茬」岗位。** 你的日常是设计题目去逼模型犯错,然后把错误归类、定位、量化,交给算法去修。

这意味着两件事:

1. **面试官会用最狠的力度问你评测。** 你答"bad case 复测"就出局了——那正是这个岗位要淘汰的做法。
2. **但你有一个别人没有的优势:你亲身吃过没有评测集的亏,并且把它想透了。** 这比背过评测理论的人可信得多。

### 9.1 你的经历 ↔ JD 要求 映射表

投递前先对一遍,知道自己哪里能打、哪里要补:

| JD 要求 | 你的筹码 | 强度 |
|---|---|---|
| 构建评测集、定义输出标准 | **知道 bad case 复测的三个缺陷,能说出评测集该怎么建**(Q22/Q23) | 🟢 强 |
| 对负面案例有敏感度和钻研精神 | 29/31 省漏检不可见、`.split()` 中文失效差两个数量级 | 🟢 **最强** |
| 发现模型在逻辑推理上的短板 | "模型不会说不知道,会编一个合理的数字" | 🟢 强 |
| 与算法协作提优化建议 | 检索召回率 vs 答案完整率的分层归因 | 🟢 强 |
| 理解模型能力边界 | "RAG 有适用规模,数据量撑不起时检索层是纯开销" | 🟢 强 |
| AI 产品实习经历 | 这个项目本身 | 🟡 中 |
| 精通 Excel、定义高质量产物 | 需现补(见 Q36) | 🔴 **要准备** |
| 金融/投行/券商背景 | 因人而异(见 Q41) | 🔴 **要准备** |

**结论:你的强项恰好压在这个岗位的主干上。** 弱项是领域知识,这是可以承认并给出补法的。

---

### Q34. ⭐ 你这个项目怎么衡量效果?

**这题在这个岗位下是生死题。** 原 Q21 的答法(承认没评测集 + 说出正解)是**及格线**,投这个岗位要往上加一层——**展示你已经把评测方法论想成了体系**。

✅ **强答(在 Q21 基础上升级):**

> "先说结论:**当时的验收方式不合格,这也是我最想来做评测岗的原因。**
>
> 当时用的是 bad case 复测——用户报上来的失败样例,改完重跑看修好几个。**这套方法有三个致命缺陷:**
>
> **一是没有分母。** 只能说修好了 18 个,说不出整体好了多少。
>
> **二是看不见的错永远看不见。** 我举个实测的例子:语料里 31 个省有 29 个提到'低空经济'。假设用户问'哪些省提到了低空经济',系统答了 8 个省,每个都有理有据。**用户会报 bad case 吗?不会。** 因为他不知道应该有 29 个。
>
> 关键在这——**'说错了'用户能发现,'漏说了'用户发现不了**。而 RAG 最主要的失效方式恰恰是漏。所以 bad case 这套方法会**系统性低估**问题严重程度。
>
> **三是过拟合。** 手里就 20 道题,反复调参让它们全对,跟背答案没区别,还没法做回归。
>
> **正确做法应该是:** 抽 100 条真实 query,人工标注每条的**关键信息点**,算信息点召回率。这样才能区分'检索没召回'和'召回了但模型没用上'——**这两个问题的解法完全不同,前者调检索,后者调 prompt。** 没有这个区分,优化就是猜。"

**🔻 追问:"那你为什么当时没建?"**
> "两个原因,一个是客观的:当时的迭代节奏是'快速验证方向是否正确',建评测集要两周,团队更倾向先改配置看效果。
>
> 但更主要的是**我当时没意识到它有多重要**。我以为验收就是'坏 case 修好了',直到复盘时才发现漏检根本不进 bad case 池。**这个认知转变是我做这个项目最大的收获。**"

*(这个答法把最大短板转化成了求职动机,而且逻辑自洽)*

---

### Q35. ⭐ 如果让你为「AI 生成 Excel 公式」建一套评测集,你怎么做?

**这是最可能的深度题**——直接考你能不能把方法论迁移到他们的场景。

✅ **强答(四步框架,与 Q23 同构):**

> "我会用和之前一样的四步,但检查点要按 Excel 的特点重新设计。
>
> **① 出题——按两个维度分层**
>
> | 任务类型 | 难度 |
> |---|---|
> | 公式生成(VLOOKUP/SUMIFS/数组) | 单步 |
> | 数据透视 | 多步依赖 |
> | 财务建模(DCF/三表联动) | 跨表引用 |
> | 数据清洗与校验 | 含异常数据 |
>
> 关键是**要专门设计陷阱题**:空值、合并单元格、跨表引用、循环引用、日期格式混乱。这些是真实办公场景的常态,也是模型最容易翻车的地方。
>
> **② 标注——拆成检查点,不是打整体分**
>
> ```
> 题目:按部门汇总 Q3 销售额，排除已退货订单
> 检查点:
>   □ 用了正确的函数(SUMIFS 而非 SUMIF)
>   □ 引用范围正确(含表头?绝对/相对引用?)
>   □ 退货条件的逻辑正确
>   □ 空值处理(会不会返回 #N/A)
>   □ 可下拉填充(引用锁定正确)
> ```
>
> 拆成点才能算分,也才能定位是哪类能力弱。
>
> **③ 指标——至少分三层,因为病根在不同层**
>
> | 指标 | 抓什么 |
> |---|---|
> | **需求理解正确率** | 有没有听懂用户要什么 |
> | **公式逻辑正确率** | 函数选型和逻辑对不对 |
> | **数值结果正确率** | 最终算出来的数对不对 |
>
> **这三层必须分开算。** 需求理解对但公式错 → 模型能力问题,给算法;需求就理解错了 → 交互/prompt 问题,产品侧能解。**这是我在 RAG 项目里学到的最有用的一条:先分清病在哪一层,再谈怎么治。**
>
> **④ 回归——每次模型更新都重跑**,并且盯**劣化项**,不能只看总分涨没涨。"

**🔻 追问:"Excel 的正确答案往往不唯一,怎么判?"**
> "**分开处理。** 数值结果是唯一的,可以自动比对——这部分能做自动化评测。公式写法不唯一,判的是'是否可接受'而不是'是否一致',要靠人工标注加规则。
>
> 我会**先做数值层的自动化**,因为它覆盖面广、成本低,能跑大批量回归;公式层用小规模人工精标,抽样做质量校准。"

---

### Q36. ⭐ 什么是一份高质量的 Excel 产物?

**JD 明写的要求,必问。** 答"格式整齐、有图表"就废了。

✅ **强答:**

> "我会拆成四个维度,**而且它们的优先级不一样**:
>
> **① 正确性(底线)** —— 数值对、函数选型对、引用范围对、边界情况不报错
>
> **② 可审计性(专业场景的核心)** —— 这条在投行/财务场景比'好看'重要一万倍:
> - 每个数字能追溯到来源,不是凭空出现
> - **不写死数字(hardcode)**——把 1234 直接敲进公式里,是财务建模的大忌
> - 假设参数集中在一处,不散落在各个公式里
> - 有中间步骤,不是一个巨型嵌套公式算到底
>
> **③ 可维护性** —— 结构清晰、命名规范、加一行数据不会崩、公式可下拉
>
> **④ 可用性** —— 单位标注、数字格式、关键假设有注释
>
> **为什么把可审计性排这么高?** 因为专业场景里 Excel 不是一个人用的——分析师做完要给 VP 复核,VP 要能一眼看出每个数字怎么来的。**一份算得对但没人敢用的模型,价值是零。**
>
> 这跟我在 RAG 项目里的判断其实是同一个:**答案对不对是一回事,能不能被验证是另一回事。** 我那个项目最该补的功能就是答案溯源——每个数字标出处块 ID,让用户一键跳原文。做不了的原因是切块时 `start_pos` 算得不准,和原文真实偏移对不上。
>
> **专业用户要的从来不是'相信 AI',是'能验证 AI'。**"

*(这个类比是你的杀手锏——它证明你对「专业场景」的理解是通的,不是临时背的)*

**🔻 追问:"那 AI 生成的 Excel,怎么保证可审计?"**
> "至少三件事:一是**强制拆步骤**,不允许输出超长嵌套公式;二是**参数区与计算区分离**,假设单独放一块;三是**生成时同步产出说明**——这个公式在干什么、引用了哪些数据。第三条本质上就是 RAG 里的引用溯源,换了个形态。"

---

### Q37. 你觉得现在的模型在什么任务上一定会出错?

**对应要求 5「对模型能力边界有较深的理解」。**

✅ **强答(先给通用规律,再落到金融场景):**

> "我从自己项目里观察到三条,**它们在办公场景同样成立:**
>
> **① 模型不会说'不知道',它会编一个看起来合理的答案。**
> 我那个场景里,检索没给到资料时,模型会编一个格式正确的数字。**这是最危险的失效方式——错得很像对的。** 换到财务场景,就是编一个看起来合理的增长率。
>
> **② 缺失比错误更难发现。**
> 29/31 省提到某个词,系统答了 8 个,用户看不出问题。对应到 Excel,就是**财务模型漏了一个科目**——表格看起来完整、数字能对平,但少了一项。
>
> **③ 长链条中间容易断,而且模型不自知。**
> 长上下文有'中间遗忘',多步计算同理——第 3 步算错,后面全部基于错误值继续推,还推得很流畅。
>
> **具体到这个岗位关注的三块短板:**
>
> | JD 提到的 | 我预期的失效模式 |
> |---|---|
> | 逻辑推理 | 多步依赖时中间步骤出错且不自知 |
> | 金融规范 | 会计准则、口径这类**知识性**内容会记混(比如收入确认时点) |
> | 复杂计算 | 嵌套层数一多就出错;跨表引用容易错行错列 |
>
> **这三类的解法完全不同**:逻辑推理要拆步骤、加自检;金融规范要外挂知识库(其实就是 RAG);复杂计算应该让模型生成公式而不是自己算——**把计算交给 Excel 引擎,模型只负责翻译需求。**"

*(最后这句是产品判断,不是技术描述,面试官会记住)*

---

### Q38. ⭐ 举一个你钻研负面案例的例子

**要求 6 直接对应,必问。** 你有两个极好的素材,**讲一个讲透,不要都讲**。

✅ **素材 A:从一个案例挖出方法论缺陷(推荐)**

> "有一次我验证'哪些省提到低空经济'这类查询。系统给的答案看起来完全正常——列了 8 个省,每个都有原文依据。
>
> **但我起了疑心:8 个会不会太少了?**
>
> 我去语料里全文检索了一遍,发现 **31 个省里有 29 个都提到了**。系统漏了 21 个省。
>
> 然后我意识到一件更严重的事:**这个错误永远不会出现在 bad case 池里。** 用户不知道正确答案是 29,他看到 8 个有理有据的省份,只会觉得'哦,原来只有这几个省'。
>
> 这让我推翻了当时的验收方法——**我们一直在修用户报上来的错,而 RAG 最主要的失效方式是漏,漏是不会被报上来的。** 从那之后我才明白,评测集不是'锦上添花的规范动作',是**唯一能看见沉默失败的手段**。"

✅ **素材 B:抠到一行代码的细节(展示 Detail-Oriented)**

> "项目里有段截断逻辑,声称按'信息密度'保留内容。我去读了打分函数:
>
> ```python
> content_score = len(content.split()) / 100
> ```
>
> **`.split()` 是按空格分词——中文没有空格。** 所以整段中文会被算成 1 个'词',这一项恒等于 0.01 左右。
>
> 我用 400 个样本实测:这一项取值范围 0.02~0.15,而同一个式子里另一项的取值范围是 0~2.0,**差了两个数量级**,等于完全不参与排序。
>
> 更关键的是验证后果:样本里数字最密集的那个块(甘肃,含 58 个数字),在原版打分下排**第 34 名**;加入数字密度后排**第 1 名**。
>
> **截断时最该保留的就是这种块,原版把它排在第 34 位。**"

**为什么这两个素材好:** 都是**从一个具体现象出发,挖到根因,再量化后果**。这正是评测岗的工作流。素材 A 的高度更高(推翻了方法论),素材 B 的细节更硬(抠到一行代码)。

**建议:先讲 A,如果面试官要"再举一个"或者追问技术细节,补 B。**

---

### Q39. 评测发现问题后,你怎么判断该改 prompt 还是找算法改模型?

**对应职责 3「与算法工程师协作,基于评测结论提出优化建议」。**

✅ **强答:**

> "**核心是分层归因——先定位病在哪一层,再决定谁来治。**
>
> 我在 RAG 项目里用的框架是:
>
> | 检索召回率 | 答案完整率 | 结论 |
> |---|---|---|
> | 低 | 低 | 资料没捞到 → **改检索** |
> | 高 | 低 | 捞到了没用好 → **改 prompt** |
> | 高 | 高 | 正常 |
>
> **没有第一列这个指标,只能靠猜。**
>
> 迁移到 Excel 场景,我会这样分:
>
> | 现象 | 归因 | 谁来治 |
> |---|---|---|
> | 需求就理解错了 | 交互设计 / prompt | **产品侧能解** |
> | 理解对了,函数选错 | 模型能力 | 给算法,或外挂函数知识库 |
> | 函数对了,引用范围错 | 上下文理解 | 给算法,或改表格结构化输入 |
> | 全对但格式不符合规范 | 后处理 | 产品侧加约束 |
>
> **提给算法的建议必须带三样东西:** 复现样例、这类问题在评测集上的占比、以及修好之后能提升多少。
>
> 只说'模型公式老出错'是没法排期的;说'嵌套三层以上的 SUMIFS 正确率只有 40%,这类题占评测集 15%,修好整体能涨 6 个点'——**这才是能进优先级的输入。**"

---

### Q40. 你平时用 Claude 或其他 AI 产品吗?遇到过它做不好的事吗?

**对应要求 5,看似闲聊,实际在考你的观察颗粒度。**

❌ **弱答:** "我用它写文档、查资料,挺好用的。"
→ 任何人都能这么说,零信息量。

✅ **强答思路:给一个具体的、你能说清失效机制的例子。**

> "用得比较多。举个最近的例子——我让它帮我分析这个 RAG 项目的代码,它能读懂调用链,但**会把'代码里写了'和'实际生效了'混为一谈**。
>
> 比如项目里有个函数写得完全正确,但主链路根本没调用它。模型看到函数存在,就默认这个功能是生效的。**它缺的是'执行路径'这一层的验证意识。**
>
> 我后来的用法是:**让它给结论的同时给出可验证的依据**——文件名和行号,我自己去核。这样它的价值从'给答案'变成'给线索',反而更可靠。
>
> 这个观察对做评测挺有用的:**模型的错误往往不是胡说,而是'合理但没验证'。** 评测集要专门设计这类题——**表面合理、实则需要执行验证的场景。**"

*(这个答案同时展示了:真实使用经验、对失效模式的洞察、以及和岗位工作的连接)*

---

### Q41. 你没有金融/投行背景,怎么做金融领域的评测?

**JD 要求 1、2 是金融背景相关。按你的实际情况选一套。**

#### 情况 A:确实没有金融背景

✅ **强答(承认 + 拆解 + 给补法):**

> "**我确实没有投行或券商的实习经历,这是我的短板。**
>
> 但我想把评测这件事拆开看——它包含两种能力:
>
> **一是方法论能力**:怎么设计题目分层、怎么把主观质量拆成可判定的检查点、怎么分层归因、怎么防止评测集过拟合。**这部分是跨领域通用的,也是我在自己项目里踩坑踩出来的。**
>
> **二是领域知识**:什么是正确的收入确认口径、DCF 的标准结构是什么、投行的建模规范。**这部分我需要补,但它是可习得的、有明确边界的。**
>
> 我的补法很具体:
> 1. **找一份真实的行业标准产物当锚**——比如公开的券商财务模型模板,拆解它的结构和规范
> 2. **和有背景的同事一起标前 20 条**,把他们的判断依据变成可复用的检查点清单
> 3. **从规则明确的子领域切入**(公式正确性、勾稽关系、数值校验),这些对错分明,不依赖行业经验;**主观判断强的部分先交给有背景的人**
>
> **我的判断是:领域知识补起来比方法论快。** 一个懂金融但没做过评测的人,容易把评测做成'凭感觉打分';而方法论扎实的人,补领域知识是有路径的。"

#### 情况 B:有相关背景(金融专业 / 券商实习)

✅ **强答(把两边接起来,这是你的独特位置):**

> "我的背景比较特殊——**既做过 AI 产品的效果优化,也有金融侧的训练**。
>
> 这个组合在评测岗上有个具体的价值:**我知道一份产物在专业上'哪里错了会致命'。**
>
> 比如财务模型里,格式不好看是小问题,但**写死数字(hardcode)、勾稽关系不平、口径前后不一致**是硬伤——一个 VP 看到就会把整份模型退回。
>
> **这种'什么错误是致命的'的判断,决定了评测集的权重怎么设。** 如果所有检查点等权,评测出来的分数是失真的——模型可能在无关紧要的项上得分很高。"

---

### Q42. 你为什么想做评测这个方向?

✅ **强答(说真话,而且这个真话特别有说服力):**

> "很直接的原因:**我在自己的项目里吃过没有评测集的亏。**
>
> 我做了一轮检索层优化,改完之后**我说不清到底好了多少**。我能说的只有'用户报的那批 case 修好了',而这批 case 本身是有偏的——漏检根本不会被报上来。
>
> 所以我做完那个项目最强的一个认知是:**AI 产品和传统产品最大的区别,是你必须先定义'怎么算好',才能开始做。** 传统功能能不能用肉眼可见,AI 的效果只存在于统计里——不建评测集,就是蒙着眼睛开车,改对了改错了都不知道。
>
> 这个岗位做的正是这件事,而且是在金融这种**对错误零容忍**的场景里。我想把在自己项目里学到的方法论,放到一个真正需要它的地方去验证。"

---

### 9.2 场景迁移速查表

**面试官很可能临时把问题切到他们的场景。** 提前把桥搭好,现场就不慌:

| 你的项目 | 豆包办公金融场景 | 共同的本质 |
|---|---|---|
| 31 省报告跨省检索 | 财报/研报跨文档提取 | 多文档信息聚合 |
| 政府报告里的精确数字 | 财务数据、公式计算 | **精确匹配比语义相似更重要** |
| 漏检对用户不可见 | 财务模型漏一个科目 | **沉默失败是最危险的** |
| 信息点拆解评分 | Excel 检查点拆解 | 主观质量要拆成可判定项 |
| 检索召回率 vs 答案完整率 | 需求理解 vs 公式逻辑 vs 数值 | **分层归因才能定位病根** |
| 答案溯源(每个数标出处) | Excel 可审计性(不 hardcode) | **专业用户要的是可验证** |
| RAG 有适用规模 | 该让模型算,还是让 Excel 算 | 认清工具的能力边界 |

**最后一行是你的高地:** RAG 项目让你形成了"**不要让模型做它不擅长的事**"的判断。在 Excel 场景里,这句话就是——**让模型生成公式,让 Excel 引擎去算。** 模型负责翻译需求,不负责做算术。

### 9.3 投这个岗位时,简历这一行怎么调

[附 D](#附-d简历怎么写) 的推荐版是通用的。投这个岗位时,**把重心从"优化"挪到"归因与评测"**:

> **政府工作报告 RAG 智能问答系统**|31 省 60 万字语料检索问答,主导检索层效果归因:通过样本实测定位配额溢出、召回缺口等结构性缺陷,并复盘设计信息点级评测方案

**改动理由:** 原版突出"设计了分层检索预算"(做了什么),这版突出"实测定位缺陷 + 设计评测方案"(**发现问题和定义标准的能力**)——正是 JD 第 2 条要的。

---

## 附 A:数据卡

面试时能张口就来的真实数字。**只说这些,别的别编。**

| 数字 | 含义 | 出处 |
|---|---|---|
| **31** | 省级行政区(不含港澳台) | `config.example.py:94` |
| **12,975** | 河南报告字数 | 实测 |
| **60 万** | 31 份原文总字数 | 实测 |
| **855** | 切块总数 | `mvp/chunks.json` |
| **20** | 河南的块数 | 实测 |
| **16,775** | 河南 20 块字数相加(含重叠) | 实测 |
| **16 / 28 / 50 / 27.6** | 各省块数 最少/中位/最多/平均 | 实测 |
| **303 / 874 / 1543** | 块长 最短/中位/最长 | 实测 |
| **22 / 31** | 总块数 ≤30 的省份数 ⭐ | 实测 |
| **29 / 31** | 提到"低空经济"的省份数 ⭐ | 实测 |
| **476 / 333 / 46 / 0** | chunk_type: target/content/summary/**title** | 实测 |
| **26%** | post-filter 的配额平均缺口 | 实测 5 条 query |
| **37.9% / 10 个省** | 扩容到 8550 块时的缺口 / 归零省份 | 规模敏感性测试 |
| **第 34 → 第 1** | 数字最密块在打分修复前后的排名 | 实测 400 样本 |
| **3 / 6** | 两处意图识别判定冲突的比例 | 实测 |
| **1000 / 200** | 切块大小 / 重叠 | `config.example.py:43-44` |
| **30 / 15 / 8 / 25** | 单省/多省/全省/对比 的每省配额 | `config.example.py:56-74` |
| **40K / 60K / 80K / 100K** | 对应的字符预算 | 同上 |

---

## 附 B:雷区清单

### 绝对不要说

| ❌ | 为什么 |
|---|---|
| "效果提升了 200%" | 那是 top_k 的提升,是**输入**不是产出 |
| "准确率 95%" | 编的数字,三层追问必塌 |
| "我用了 RAG 技术" | 等于说"我用了数据库",2023 年的话术 |
| "我做了创新的分层检索架构" | 这不是创新,是按场景配参数。说创新会引来更狠的拷问 |
| "向量是 1024 维" | 那只是代码里的默认参数,实际是运行时探测的 |
| "1000 字是最优的语义长度" | 编的理由,问一句"怎么验证的"就完了 |
| **"我们做了详细的模型对比评测"** | 仓库里没有任何选型文档。追问"评测数据呢"就塌 |

### 会引火烧身的表述

- **"这个项目很完善"** → 面试官立刻开始找漏洞,而且你失去了主动承认的机会
- **"没遇到什么问题"** → 要么没深入做,要么不诚实
- **"用户反馈很好"** → 追问"好在哪、怎么量化的"

### 主动认三件事(先认的坑不是坑)

1. **没有评测集** —— 但说得出该怎么建
2. **相邻块聚合是补丁不是根治** —— 根因是固定长度切块,正解是语义切分
3. **"提升 200%"是配置口径** —— 如果面试官翻到了 README,你要能立刻接住
4. **模型选型偏重了** —— Jina v4 的多模态和 32K 上下文一项没用上,BGE-M3 更契合(见 Q7)

---

## 附 C:30 秒项目自述

背熟这一段,面试开场直接用:

> "31 个省的政府工作报告,60 万字。用户是政策研究员,做跨省对比以前要通读所有文档、两到四个小时。
>
> 系统上线后收到两类反馈:**答不全**,和**多省对比时每个省只给一句话**。
>
> 我做的是**检索层的归因和优化**——不是搭链路,是修效果。抽样 bad case 后确认是检索层的预算问题,不是生成模型的问题。然后做了三件事:按查询类型分层分配检索预算、相邻块聚合、信息密度优先的截断。
>
> **但我后来复盘发现,当时的解法是对的方向、粗放的手段。** 实测下来,31 个省里有 22 个省的总块数不到单省查询的配额——**单省查询时检索层根本没在筛选,等于全文投喂**。
>
> 这让我形成一个判断:**RAG 不是越用越好,它有适用规模。**"

**为什么这段有效:** 前半段讲清做了什么,后半段主动亮出反思。面试官听完会想继续问,而不是想挑刺。

---

## 附 D:简历怎么写

> 时间顺序上这一节最先用到——**先有简历,才有面试**。写完简历再回头核对本手册其余部分。

### 推荐版本

> **政府工作报告 RAG 智能问答系统**|31 省 60 万字语料的跨省检索问答,主导检索层问题归因与优化,设计按查询类型分层的检索预算方案,解决多省对比查询信息缺失问题

**每个短语为什么这么写:**

| 短语 | 作用 | 可验证吗 |
|---|---|---|
| 31 省 60 万字 | 给出规模,一眼看出不是玩具项目 | ✅ 实测 |
| **主导检索层归因与优化** | 说清角色——不是搭链路,是修效果 | ✅ 真实职责 |
| 按查询类型分层的检索预算 | 具体设计动作,能往下讲 | ✅ 30/15/8/25 |
| 解决多省对比查询信息缺失 | 指向真实问题,不吹结果 | ✅ 用户反馈 |

### 两个变体

**偏产品岗**(强调价值和用户):

> **政府工作报告 RAG 智能问答系统**|面向政策研究场景,将 31 省 60 万字报告的跨省对比从人工通读压缩至分钟级;主导检索层归因与优化,解决多省查询「答不全」问题

**偏技术产品岗**(强调工程判断):

> **政府工作报告 RAG 智能问答系统**|31 省 60 万字语料检索问答,主导检索层归因:定位并修复检索预算分配、省份过滤时序等结构性缺陷,建立按查询类型分层的检索策略

### ❌ 绝对不要写

> ~~"通过智能分层检索优化,召回块数提升 200%,上下文窗口扩展 525%,信息完整度提升 100%"~~

这是 README 里的原始口径。**三个数字全是过程指标**——top_k 从 20 调到 60 是**输入**,不是产出。面试官下一句必定是"那效果提升了多少",而你没有评测集,答不上来。

**简历上的每个数字,面试官都会当成你验证过的结论来追问。** 写了不能证明的数字,等于给自己埋雷。

### ⭐ 简历短语 ↔ 手册题目对照

简历这一行的作用是**拿到面试**,不是赢下面试。它只需做到两件事:让人想问,以及**被问到时你全都答得上来**。

推荐版的每个短语,都对应手册里准备好的题:

| 简历里写的 | 面试官会问 | 看手册哪题 |
|---|---|---|
| 31 省 60 万字 | 规模、语料来源、怎么处理的 | [附 A 数据卡](#附-a数据卡)、Q4~Q6 |
| 检索层归因 | 怎么定位的、怎么证明有效 | **Q21**(怎么证明有效)、**Q22**(bad case 的问题) |
| 分层检索预算 | 30/15/8/25 怎么定的 | Q10、**Q13**(单省实际返回多少块)← 高光时刻 |
| 多省查询信息缺失 | 缺在哪、修好了吗 | Q12(全省查询 8 块够吗) |

**投递前自查:** 简历上出现的每个名词,你都能在手册里找到对应答案吗?找不到的,要么删掉,要么补上准备。

---

## 最后:如果只记三件事

1. **四个框架**比 40 个答案有用。数字类坦白 + 实测 + 正解;选型类约束 + 候选 + 判据 + 代价;效果类诚实 + 缺陷 + 正解;反思类只说架构判断。

2. **22/31 这个数字要脱口而出。** 它是你整场面试最有力的一句话,能同时证明你读过代码、做过实测、有产品判断。

3. **不编数字。** 这场面试你唯一会挂的方式,就是编一个数字然后被追到第三层。诚实说"没验证"永远比编一个数字安全。

---

## 投特定岗位时的读法

通用部分(第 0~8 部分)是底子,**但不同岗位的考察重心差别很大**。

已收录的岗位定向:

| 岗位 | 章节 | 考察重心 | 你要重点准备 |
|---|---|---|---|
| 字节 · 豆包办公 AI PM(金融评测) | [第 9 部分](#第-9-部分岗位定向--字节豆包办公-ai-pm金融评测方向) | **评测方法论 + 负面案例敏感度** | Q34、Q35、Q36、Q38 |

**读法建议:** 先把第 0 部分(四个框架)和第 6 部分(评估)吃透,再看第 9 部分的定向题——第 9 部分的答案都是在通用答案上"加一层",不是另起炉灶。
